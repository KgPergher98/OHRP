# Core numerical stack
# pandas 3 changes how returns are computed across price gaps and breaks the
# label handling of OHRP/S-OHRP, so it is excluded.
# For the exact environment that produced the dissertation results, use
# requirements-dissertation.txt instead (see "Reproducibility" in the README).
pandas>=2.0,<3
numpy>=1.24
scipy>=1.10

# Excel reader for datasets/SectoralBrazilianClassification.xlsx
openpyxl>=3.1

# Figures produced by analise_experimentos.py and setores_b3.py
matplotlib>=3.7

# Risk Parity benchmark (RP.py). Everything else runs without it.
riskfolio-lib>=7.0

# Only for the IEEE Access notebooks (tutorialDataExtraction.ipynb and
# tutorialHyperparameterSensibility.ipynb)
yfinance
requests
seaborn
